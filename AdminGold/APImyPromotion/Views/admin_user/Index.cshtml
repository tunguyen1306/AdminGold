@model IEnumerable<APImyPromotion.Models.tbl_user_promotion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Email
        </th>
        <th>
           Điện thoại
        </th>
        <th>
           Tên
        </th>
        <th>
           Họ
        </th>
        <th>
           Loại User
        </th>
        <th>
         Trạng thái
        </th>
        <th>
           Mật khẩu
        </th>
        <th>
           Hình
        </th>
        <th>
           Họ và tên
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.email_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phone_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.first_name_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.last_name_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type_role_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pass_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.img_user_promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.full_name_user_promotion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_user_promotion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_user_promotion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_user_promotion })
            <a href="@Url.Action("Delete",new { id=item.id_user_promotion })" onclick="return confirm('Bạn có muốn xóa không ?')"></a>
        </td>
    </tr>
}

</table>
