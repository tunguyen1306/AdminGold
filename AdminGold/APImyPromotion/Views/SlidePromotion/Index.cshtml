@model IEnumerable<APImyPromotion.Models.tbl_slide_promotion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.img_slide)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.title_slide)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.des_slide)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status_slide)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.img_slide)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.title_slide)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.des_slide)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status_slide)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_slide }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_slide }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_slide })
        </td>
    </tr>
}

</table>
